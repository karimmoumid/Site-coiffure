<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #000; color: #FFD700; padding: 20px; text-center; }
        .content { background: #f8f9fa; padding: 30px; }
        .info-box { background: white; border: 2px solid #FFD700; padding: 15px; margin: 20px 0; }
        .urgent { background: #fff3cd; border-left: 4px solid #ffc107; padding: 10px; margin: 15px 0; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h2>🔔 Nouvelle Demande de Rendez-vous</h2>
        </div>
        
        <div class="content">
            <div class="urgent">
                <strong>⚠️ Action requise :</strong> Une nouvelle demande de rendez-vous nécessite votre attention.
            </div>
            
            <div class="info-box">
                <h3 style="color: #FFD700; margin-top: 0;">Informations du Client</h3>
                <p><strong>Nom :</strong> {{ appointment.clientName }}</p>
                <p><strong>Email :</strong> {{ appointment.clientEmail }}</p>
                <p><strong>Téléphone :</strong> {{ appointment.clientPhone }}</p>
            </div>
            
            <div class="info-box">
                <h3 style="color: #FFD700; margin-top: 0;">Détails du Rendez-vous</h3>
                <p><strong>Service :</strong> {{ appointment.service.name }}</p>
                <p><strong>Catégorie :</strong> {{ appointment.service.category.name }}</p>
                <p><strong>Date souhaitée :</strong> {{ appointment.appointmentDate|date('d/m/Y') }}</p>
                <p><strong>Heure souhaitée :</strong> {{ appointment.appointmentTime|date('H:i') }}</p>
                <p><strong>Durée :</strong> {{ appointment.service.duration }} minutes</p>
                <p><strong>Prix :</strong> {{ appointment.service.price }} €</p>
                {% if appointment.notes %}
                <p><strong>Notes du client :</strong><br>{{ appointment.notes }}</p>
                {% endif %}
                <p><strong>Date de la demande :</strong> {{ appointment.createdAt|date('d/m/Y H:i') }}</p>
            </div>
            
            <p style="text-align: center; margin-top: 30px;">
                <strong>Connectez-vous au dashboard pour confirmer ou refuser ce rendez-vous.</strong>
            </p>
        </div>
    </div>
</body>
</html>
