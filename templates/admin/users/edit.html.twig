{% extends 'base.html.twig' %}

{% block title %}Modifier Utilisateur - Admin{% endblock %}

{% block body %}
<div class="container-fluid my-4">
    <div class="row">
        <!-- Sidebar -->
        {% include 'admin/components/sidebar.html.twig' %}

        <!-- Main content -->
        <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
            <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
                <h1 class="h2">
                    <i class="bi bi-person-gear text-gold"></i> Modifier l'Utilisateur
                </h1>
                <a href="{{ path('admin_users') }}" class="btn btn-black">
                    <i class="bi bi-arrow-left"></i> Retour à la liste
                </a>
            </div>

            <div class="row justify-content-center">
                <div class="col-lg-6">
                    <div class="card shadow">
                        <div class="card-header bg-dark text-gold">
                            <h5 class="mb-0">
                                <i class="bi bi-person-fill-gear"></i> Modifier le compte de {{ user.name }}
                            </h5>
                        </div>
                        <div class="card-body p-4">
                            <!-- Informations actuelles -->
                            <div class="alert alert-light mb-4">
                                <h6 class="alert-heading">
                                    <i class="bi bi-info-circle"></i> Informations actuelles
                                </h6>
                                <div class="row">
                                    <div class="col-md-6">
                                        <small class="text-muted">ID :</small> #{{ user.id }}<br>
                                        <small class="text-muted">Créé le :</small> 
                                        {% if user.createdAt %}
                                            {{ user.createdAt|date('d/m/Y H:i') }}
                                        {% else %}
                                            <span class="text-muted">-</span>
                                        {% endif %}
                                    </div>
                                    <div class="col-md-6">
                                        <small class="text-muted">Dernière connexion :</small> 
                                        {% if user.lastLoginAt %}
                                            {{ user.lastLoginAt|date('d/m/Y H:i') }}
                                        {% else %}
                                            <span class="text-muted">Jamais</span>
                                        {% endif %}
                                        <br>
                                        <small class="text-muted">Rôle actuel :</small>
                                        {% if 'ROLE_ADMIN' in user.roles %}
                                            <span class="badge bg-danger">Admin</span>
                                        {% elseif 'ROLE_EMPLOYEE' in user.roles %}
                                            <span class="badge bg-warning text-dark">Employé</span>
                                        {% else %}
                                            <span class="badge bg-info">Utilisateur</span>
                                        {% endif %}
                                    </div>
                                </div>
                            </div>
                            
                            {{ form_start(form) }}
                            
                            <div class="mb-3">
                                <label class="form-label fw-bold">
                                    <i class="bi bi-person"></i> {{ form_label(form.name) }} 
                                    <span class="text-danger">*</span>
                                </label>
                                {{ form_widget(form.name) }}
                                {{ form_errors(form.name) }}
                            </div>
                            
                            <div class="mb-3">
                                <label class="form-label fw-bold">
                                    <i class="bi bi-envelope"></i> {{ form_label(form.email) }} 
                                    <span class="text-danger">*</span>
                                </label>
                                {{ form_widget(form.email) }}
                                {{ form_errors(form.email) }}
                                <small class="form-text text-muted">
                                    L'email sera utilisé pour la connexion
                                </small>
                            </div>
                            
                            <div class="mb-3">
                                <label class="form-label fw-bold">
                                    <i class="bi bi-lock"></i> {{ form_label(form.plainPassword) }}
                                </label>
                                {{ form_widget(form.plainPassword) }}
                                {{ form_errors(form.plainPassword) }}
                                <small class="form-text text-warning">
                                    <i class="bi bi-exclamation-triangle"></i> 
                                    Laissez vide pour conserver le mot de passe actuel
                                </small>
                            </div>
                            
                            <div class="mb-4">
                                <label class="form-label fw-bold">
                                    <i class="bi bi-shield"></i> {{ form_label(form.userType) }} 
                                    <span class="text-danger">*</span>
                                </label>
                                {{ form_widget(form.userType) }}
                                {{ form_help(form.userType) }}
                                {{ form_errors(form.userType) }}
                            </div>
                            
                            <hr>
                            
                            <div class="alert alert-warning">
                                <i class="bi bi-exclamation-triangle"></i> 
                                <strong>Attention :</strong>
                                <ul class="mb-0 mt-2">
                                    <li>Modifier le rôle d'un utilisateur changera ses permissions immédiatement</li>
                                    <li>Si vous changez le mot de passe, l'utilisateur devra se reconnecter</li>
                                    <li>L'email doit être unique dans le système</li>
                                </ul>
                            </div>
                            
                            <div class="d-grid gap-2">
                                <button type="submit" class="btn btn-gold btn-lg">
                                    <i class="bi bi-check-circle"></i> Enregistrer les modifications
                                </button>
                                <a href="{{ path('admin_users') }}" class="btn btn-secondary">
                                    <i class="bi bi-x-circle"></i> Annuler
                                </a>
                            </div>
                            
                            {{ form_end(form) }}
                        </div>
                    </div>
                    
                    <!-- Zone de danger -->
                    <div class="card mt-4 border-danger">
                        <div class="card-header bg-danger text-white">
                            <h6 class="mb-0">
                                <i class="bi bi-exclamation-triangle"></i> Zone de danger
                            </h6>
                        </div>
                        <div class="card-body">
                            <p>Supprimer définitivement cet utilisateur de la base de données.</p>
                            <p class="text-danger mb-3">
                                <small>Cette action est irréversible et supprimera toutes les données associées.</small>
                            </p>
                            <button type="button" 
                                    class="btn btn-danger" 
                                    data-bs-toggle="modal" 
                                    data-bs-target="#deleteUserModal">
                                <i class="bi bi-trash"></i> Supprimer cet utilisateur
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </main>
    </div>
</div>

<!-- Modal de confirmation de suppression -->
<div class="modal fade" id="deleteUserModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-danger text-white">
                <h5 class="modal-title">
                    <i class="bi bi-exclamation-triangle"></i> Confirmation de suppression
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <p>Êtes-vous sûr de vouloir supprimer définitivement l'utilisateur <strong>{{ user.name }}</strong> ?</p>
                <p class="text-danger">
                    <i class="bi bi-exclamation-circle"></i> Cette action est irréversible et supprimera :
                </p>
                <ul>
                    <li>Le compte utilisateur</li>
                    <li>Toutes les données associées</li>
                    <li>L'historique de connexion</li>
                </ul>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                    <i class="bi bi-x"></i> Annuler
                </button>
                <form method="post" action="{{ path('admin_user_delete', {id: user.id}) }}" style="display: inline;">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
                    <button type="submit" class="btn btn-danger">
                        <i class="bi bi-trash"></i> Supprimer définitivement
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>

<style>
    .form-control:focus, .form-select:focus {
        border-color: var(--gold);
        box-shadow: 0 0 0 0.2rem rgba(255, 215, 0, 0.25);
    }
    
    .sidebar {
        background-color: var(--black) !important;
    }
    
    .sidebar .nav-link {
        padding: 0.75rem 1rem;
        margin-bottom: 0.25rem;
        border-left: 3px solid transparent;
        transition: all 0.3s;
    }
    
    .sidebar .nav-link:hover {
        background-color: var(--dark-gray);
        border-left-color: var(--gold);
    }
    
    .sidebar .nav-link.active {
        background-color: var(--dark-gray);
        border-left-color: var(--gold);
        color: var(--gold) !important;
    }
</style>
{% endblock %}
